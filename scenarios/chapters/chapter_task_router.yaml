enabled: true
name: Chapter task router

node: main_city

steps:
  # `chapter.task` is populated by the overlay text rule before this router is enqueued.
  - cond: chapter.task ~= "Upgrade"
    steps:
      - click: chapter.task
      - wait: 2s
      - push_scenario:
          name: upgrade
          priority: 80000
