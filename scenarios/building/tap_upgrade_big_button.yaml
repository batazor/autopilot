enabled: true
name: Tap upgrade big button
# Overlay fires when the icon is visible; double template pass reduces false taps.
device_level: true

steps:
  - while_match: upgrade_big_button
    max: 1
    steps:
      - click: upgrade_big_button
      - wait: 800ms
