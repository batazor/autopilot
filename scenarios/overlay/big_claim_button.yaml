enabled: true
name: Tap big claim button
# Popup / full-width claim — UI only, no player identity needed.
device_level: true

steps:
  - while_match: big_claim_button
    steps:
      - click: big_claim_button
      - wait: 1s
