enabled: true
name: ✅ Tap confirm (blue) button
# Popup dismissal — purely UI, no player identity needed.
device_level: true

# Triggered by overlay rule `ui.button.confirm_blue.visible` (priority 88_000).

steps:
  - match: ui.button.confirm_blue
    threshold: 0.92
    min_match_saturation: 48
  - click: ui.button.confirm_blue
  - wait: 1500ms
