enabled: true
name: "🎒 Open Backpack"

node: main_city

# Pushed by ``main_city.to.backpack.has_red_dot`` when the backpack button on
# main_city lights up. Just opens the page — claim flow is handled by
# overlays on the backpack screen.

steps:
  - click: main_city.to.backpack
  - wait: 2s
  - exec: put_all_red_dots
