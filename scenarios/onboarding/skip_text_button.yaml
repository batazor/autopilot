enabled: true
name: ⏭️ Tap skip (text screen)
# Tutorial dismissal — purely UI, safe to run before player identity is known.
device_level: true

steps:
  - while_match: skip_text_button
    max: 1
    steps:
      - click: skip_text_button
      - wait: 2s
