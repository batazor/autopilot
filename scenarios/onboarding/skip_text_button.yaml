enabled: true
name: Tap skip (text screen)
# Tutorial dismissal — purely UI, safe to run before player identity is known.
device_level: true

steps:
  - match: skip_text_button
    threshold: 0.95
  - click: skip_text_button
  - wait: 2s
