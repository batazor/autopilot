enabled: true
name: Tap tutorial hand pointer (small reverse)
# Tutorial dismissal — purely UI, safe to run before player identity is known.
device_level: true

steps:
  - match: hand_pointer_small_reverse
    threshold: 0.9
  - click: hand_pointer_small_reverse
