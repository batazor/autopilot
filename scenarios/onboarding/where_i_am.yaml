enabled: true
name: Where am I — detect current page
# Bootstraps `current_screen` — must be runnable before any player is identified.
device_level: true
# Skip when we already know the node (empty / none / unknown match overlay `screens: [none]`).
cond: currentNode == none

steps:
  - exec: detect_screen
