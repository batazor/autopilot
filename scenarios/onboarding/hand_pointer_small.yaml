enabled: true
name: 👆 Tap tutorial hand pointer (small)
# Tutorial dismissal — purely UI, safe to run before player identity is known.
device_level: true

steps:
  - while_match: hand_pointer_small
    max: 1
    steps:
      - click: hand_pointer_small
