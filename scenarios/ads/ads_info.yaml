enabled: true
name: "Dismiss ads info"
# Ads dismissal — purely UI, no player identity needed.
device_level: true

steps:
  - while_match: ads.info
    max: 10
    steps:
      - click: ads.info
      - wait: 1s
