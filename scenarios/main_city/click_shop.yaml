enabled: true
name: "Open Shop page"

node: main_city

# Pushed by ``page.shop.has_red_dot`` when the Shop button on main_city
# lights up. Just opens the page — claim/buy flow is handled by overlays on
# `shop`.

steps:
  - click: page.shop
  - wait: 1s
