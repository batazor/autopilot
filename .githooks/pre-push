#!/bin/sh
# Local safety net while CI is down (GHA billing): lint before any push.
# Tests are NOT run here — they're too slow for a push gate; run them with
# `uv run pytest -q` yourself. Skip the lint in a hurry with `git push --no-verify`.
set -e
echo "pre-push: ruff"
uv run ruff check .
