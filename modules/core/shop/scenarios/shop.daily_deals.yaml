enabled: true
name: "Shop: Daily Deals"
priority: 80_000

node: shop.daily_deals

steps:
  - while_match: box.free
    max: 1
    retry:
      attempts: 5
      interval: 300ms
    steps:
      - click: box.free
      - wait: 1s
