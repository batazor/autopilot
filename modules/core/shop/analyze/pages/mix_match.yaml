overlay:
  - name: shop.mix_match.page
    region: page.shop.mix_match.title
    action: findIcon
    threshold: 0.9
    screens:
      - shop.mix_match
    pushScenario:
      - name: shop.mix_match
