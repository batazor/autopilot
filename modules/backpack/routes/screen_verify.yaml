screens:
  backpack:
    priority: 30
    rules:
    - match: backpack.title
      threshold: 0.9
  backpack.resources:
    priority: 15
    rules:
    - match: backpack.title
      threshold: 0.9
      tab_active: page.backpack.resources
  backpack.speedup:
    priority: 15
    rules:
    - match: backpack.title
      threshold: 0.9
      tab_active: page.backpack.speedup
  backpack.bonus:
    priority: 15
    rules:
    - match: backpack.title
      threshold: 0.9
      tab_active: page.backpack.bonus
  backpack.gear:
    priority: 15
    rules:
    - match: backpack.title
      threshold: 0.9
      tab_active: page.backpack.gear
  backpack.other:
    priority: 15
    rules:
    - match: backpack.title
      threshold: 0.9
      tab_active: page.backpack.other
