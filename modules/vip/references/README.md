VIP reference screenshots and crops live here when the VIP module owns them.

Use the annotator UI to add or update captures:

```bash
uv run streamlit run ui/area_annotator.py
```

Do not hand-edit `area.json`; export regions and crops from the annotator.
