[server]
# Do not auto-open a browser tab on startup (use the printed URL).
headless = true
# Rolling preview rewrites references/temporal/*.png every second; watching
# repo files makes Streamlit burn CPU and starves the embedded worker.
fileWatcherType = "none"

[browser]
gatherUsageStats = false

