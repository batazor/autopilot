overlay:
  - name: page.worker.add.visible
    region: page.worker.add
    action: findIcon
    threshold: 0.9
    screens:
      - survivor_status
    ttl: 1m
    pushScenario:
      - name: assign_worker
        priority: 80_000
