# No overlay rule for the marksman ready-bubble: the collect hint (white speech
# bubble + animated hand) pulses, so a findIcon template is unreliable. The
# main_menu panel scanner is the trigger instead — a Training/marksman row in the
# "Completed" state pushes accept_troops_marksman, which collects and then chains
# into troops.marksman.train.
overlay: []
