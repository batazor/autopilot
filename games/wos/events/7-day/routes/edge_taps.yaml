edges:
  main_city:
    event.7-day:
      resolver: template_icon
      region: main_city.icon_search
      template: games/wos/events/7-day/references/main_city.event.7-day.png
      threshold: 0.9

  event.7-day:
    # Close via the panel's top-right «×» (event.7-day.close, centre ≈ 678,349),
    # NOT icon.page.back — the 7-Day popup has no top-left back arrow, so the old
    # blind tap landed at 44,55 and missed.
    main_city: [event.7-day.close]
