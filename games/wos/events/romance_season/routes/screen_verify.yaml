screens:
  event.romance_season:
    rules:
      - match: romance_season.title
        threshold: 0.9

  romance_season.target:
    priority: 5
    rules:
      - match: romance_season.attack
        threshold: 0.9

  romance_season.rosarion:
    priority: 20
    terminal: true
    rules:
      - match: rosarion.title
        threshold: 0.9
