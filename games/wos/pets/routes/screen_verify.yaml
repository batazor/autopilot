screens:
  pets:
    rules:
      - match: pets.skills
        threshold: 0.9
  pets.pet_skill:
    # Reward popup; pets.pet_skill scenario claims + tapanywhereyoexit to dismiss.
    terminal: true
    rules:
      - match: pet.skill.title
        threshold: 0.9
