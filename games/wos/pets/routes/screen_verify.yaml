screens:
  pets:
    rules:
      - match: pets.skills
        threshold: 0.9
  pets.pet_skill:
    # Reward popup; pets.pet_skill scenario claims + tapanywhereyoexit to dismiss.
    terminal: true
    rules:
      - ocr: pet.skill.title
        contains: "Pet Skill"
