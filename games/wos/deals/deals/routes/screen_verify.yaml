families:
  deals:
    hub: deals
    prefix: deals.
    tab_region: deals.tabs_strip
    namespace: deals
    advance_scenario: tabs.strip.advance
    next_region: deals.next.left

screens:
  deals:
    priority: 60
    rules:
    # RU guess «Сделки» (Deals hub) — VERIFY LIVE (could be «Акции»/«Предложения»).
    - ocr: page.common.title
      contains: [Deals, Сделки]
      threshold: 0.8
