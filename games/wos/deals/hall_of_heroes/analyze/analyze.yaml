overlay:
  # The ``+`` button in the rewards panel renders with a tiny red dot when
  # there is something new to claim on the Witness sub-page. The dot is too
  # small for ``find_red_dots`` HSV/circularity gates, so we use the labeled
  # bbox as a template instead — ``findIcon`` only fires when the button is
  # drawn in the "with red dot" variant.
  #
  # When matched, push two scenarios:
  #   1. ``deals.hall_of_heroes`` — runs in place on the hub (dismiss the tips
  #      overlay, OCR the event score). Pushed first so it reads the hub before
  #      witness navigates away.
  #   2. ``hall_of_heroes.witness`` — the scheduler navigates via the
  #      ``deals.hall_of_heroes → hall_of_heroes.witness`` edge (taps the same
  #      button) and then runs the ``while_match: button.free`` claim loop.
  - name: deals.hall_of_heroes.add.has_dot
    region: deals.hall_of_heroes.add
    action: findIcon
    threshold: 0.9
    screens:
      - deals.hall_of_heroes
    steps:
      - push_scenario:
          name: deals.hall_of_heroes
          ttl: 1m
      - push_scenario:
          name: hall_of_heroes.witness
          ttl: 1m
