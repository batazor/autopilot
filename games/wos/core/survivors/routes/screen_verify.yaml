screens:
  isNewPeople:
    priority: 5
    rules:
    - match: button.welcome_in
      threshold: 0.9
  survivor_status:
    priority: 9
    rules:
    - ocr: survivor_status.title
      # RU guess «Статус выживших» (token «выживших»; chapter tab reads «Выживший» on bs5) — VERIFY LIVE.
      contains: ["Survivor Status", выживших]
  survivor_status.status:
    priority: 8
    rules:
    - match: survivor_status.title
      threshold: 0.9
      tab_active: survivor_status.status
  survivor_status.details:
    priority: 8
    rules:
    - match: survivor_status.title
      threshold: 0.9
      tab_active: survivor_status.details
