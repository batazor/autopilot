# Generic reward popup exit. Some VIP-styled reward dialogs render
# "Click to continue" but ignore coordinate taps; Android BACK closes them.
edges:
  rewards:
    main_city:
      - type: system_back
