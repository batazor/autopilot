edges:
  main_city:
    heroes: [main_city.to.heroes]

  heroes:
    main_city: [icon.page.back]
    hero.recrutment: [heroes.recruit]
    page.heroes.ahmose: { resolver: hero_grid, target: ahmose }
    page.heroes.aisling: { resolver: hero_grid, target: aisling }
    page.heroes.alonso: { resolver: hero_grid, target: alonso }
    page.heroes.bahiti: { resolver: hero_grid, target: bahiti }
    page.heroes.blanchette: { resolver: hero_grid, target: blanchette }
    page.heroes.bradley: { resolver: hero_grid, target: bradley }
    page.heroes.cara: { resolver: hero_grid, target: cara }
    page.heroes.charlie: { resolver: hero_grid, target: charlie }
    page.heroes.cloris: { resolver: hero_grid, target: cloris }
    page.heroes.dominic: { resolver: hero_grid, target: dominic }
    page.heroes.edith: { resolver: hero_grid, target: edith }
    page.heroes.eleonora: { resolver: hero_grid, target: eleonora }
    page.heroes.elif: { resolver: hero_grid, target: elif }
    page.heroes.estrella: { resolver: hero_grid, target: estrella }
    page.heroes.eugene: { resolver: hero_grid, target: eugene }
    page.heroes.flint: { resolver: hero_grid, target: flint }
    page.heroes.flora: { resolver: hero_grid, target: flora }
    page.heroes.fred: { resolver: hero_grid, target: fred }
    page.heroes.freya: { resolver: hero_grid, target: freya }
    page.heroes.gatot: { resolver: hero_grid, target: gatot }
    page.heroes.gina: { resolver: hero_grid, target: gina }
    page.heroes.gisela: { resolver: hero_grid, target: gisela }
    page.heroes.gordon: { resolver: hero_grid, target: gordon }
    page.heroes.greg: { resolver: hero_grid, target: greg }
    page.heroes.gregory: { resolver: hero_grid, target: gregory }
    page.heroes.gwen: { resolver: hero_grid, target: gwen }
    page.heroes.hank: { resolver: hero_grid, target: hank }
    page.heroes.hector: { resolver: hero_grid, target: hector }
    page.heroes.hendrik: { resolver: hero_grid, target: hendrik }
    page.heroes.hervor: { resolver: hero_grid, target: hervor }
    page.heroes.jasser: { resolver: hero_grid, target: jasser }
    page.heroes.jeronimo: { resolver: hero_grid, target: jeronimo }
    page.heroes.jessie: { resolver: hero_grid, target: jessie }
    page.heroes.karol: { resolver: hero_grid, target: karol }
    page.heroes.ligeia: { resolver: hero_grid, target: ligeia }
    page.heroes.ling_xue: { resolver: hero_grid, target: ling_xue }
    page.heroes.lloyd: { resolver: hero_grid, target: lloyd }
    page.heroes.logan: { resolver: hero_grid, target: logan }
    page.heroes.lumak_bokan: { resolver: hero_grid, target: lumak_bokan }
    page.heroes.lynn: { resolver: hero_grid, target: lynn }
    page.heroes.magnus: { resolver: hero_grid, target: magnus }
    page.heroes.mia: { resolver: hero_grid, target: mia }
    page.heroes.molly: { resolver: hero_grid, target: molly }
    page.heroes.natalia: { resolver: hero_grid, target: natalia }
    page.heroes.norah: { resolver: hero_grid, target: norah }
    page.heroes.patrick: { resolver: hero_grid, target: patrick }
    page.heroes.philly: { resolver: hero_grid, target: philly }
    page.heroes.reina: { resolver: hero_grid, target: reina }
    page.heroes.renee: { resolver: hero_grid, target: renee }
    page.heroes.rufus: { resolver: hero_grid, target: rufus }
    page.heroes.seigel: { resolver: hero_grid, target: seigel }
    page.heroes.seo_yoon: { resolver: hero_grid, target: seo_yoon }
    page.heroes.sergey: { resolver: hero_grid, target: sergey }
    page.heroes.smith: { resolver: hero_grid, target: smith }
    page.heroes.sonya: { resolver: hero_grid, target: sonya }
    page.heroes.ursar: { resolver: hero_grid, target: ursar }
    page.heroes.viveca: { resolver: hero_grid, target: viveca }
    page.heroes.vulcanus: { resolver: hero_grid, target: vulcanus }
    page.heroes.wayne: { resolver: hero_grid, target: wayne }
    page.heroes.wu_ming: { resolver: hero_grid, target: wu_ming }
    page.heroes.xura: { resolver: hero_grid, target: xura }
    page.heroes.zinman: { resolver: hero_grid, target: zinman }

  hero.recrutment:
    main_city: [icon.page.back]

  heroes.sr.new:
    main_city: [heroes.sr.new.close]

  page.heroes.unit: { heroes: [icon.page.back] }
  page.heroes.ahmose: { heroes: [icon.page.back] }
  page.heroes.aisling: { heroes: [icon.page.back] }
  page.heroes.alonso: { heroes: [icon.page.back] }
  page.heroes.bahiti: { heroes: [icon.page.back] }
  page.heroes.blanchette: { heroes: [icon.page.back] }
  page.heroes.bradley: { heroes: [icon.page.back] }
  page.heroes.cara: { heroes: [icon.page.back] }
  page.heroes.charlie: { heroes: [icon.page.back] }
  page.heroes.cloris: { heroes: [icon.page.back] }
  page.heroes.dominic: { heroes: [icon.page.back] }
  page.heroes.edith: { heroes: [icon.page.back] }
  page.heroes.eleonora: { heroes: [icon.page.back] }
  page.heroes.elif: { heroes: [icon.page.back] }
  page.heroes.estrella: { heroes: [icon.page.back] }
  page.heroes.eugene: { heroes: [icon.page.back] }
  page.heroes.flint: { heroes: [icon.page.back] }
  page.heroes.flora: { heroes: [icon.page.back] }
  page.heroes.fred: { heroes: [icon.page.back] }
  page.heroes.freya: { heroes: [icon.page.back] }
  page.heroes.gatot: { heroes: [icon.page.back] }
  page.heroes.gina: { heroes: [icon.page.back] }
  page.heroes.gisela: { heroes: [icon.page.back] }
  page.heroes.gordon: { heroes: [icon.page.back] }
  page.heroes.greg: { heroes: [icon.page.back] }
  page.heroes.gregory: { heroes: [icon.page.back] }
  page.heroes.gwen: { heroes: [icon.page.back] }
  page.heroes.hank: { heroes: [icon.page.back] }
  page.heroes.hector: { heroes: [icon.page.back] }
  page.heroes.hendrik: { heroes: [icon.page.back] }
  page.heroes.hervor: { heroes: [icon.page.back] }
  page.heroes.jasser: { heroes: [icon.page.back] }
  page.heroes.jeronimo: { heroes: [icon.page.back] }
  page.heroes.jessie: { heroes: [icon.page.back] }
  page.heroes.karol: { heroes: [icon.page.back] }
  page.heroes.ligeia: { heroes: [icon.page.back] }
  page.heroes.ling_xue: { heroes: [icon.page.back] }
  page.heroes.lloyd: { heroes: [icon.page.back] }
  page.heroes.logan: { heroes: [icon.page.back] }
  page.heroes.lumak_bokan: { heroes: [icon.page.back] }
  page.heroes.lynn: { heroes: [icon.page.back] }
  page.heroes.magnus: { heroes: [icon.page.back] }
  page.heroes.mia: { heroes: [icon.page.back] }
  page.heroes.molly: { heroes: [icon.page.back] }
  page.heroes.natalia: { heroes: [icon.page.back] }
  page.heroes.norah: { heroes: [icon.page.back] }
  page.heroes.patrick: { heroes: [icon.page.back] }
  page.heroes.philly: { heroes: [icon.page.back] }
  page.heroes.reina: { heroes: [icon.page.back] }
  page.heroes.renee: { heroes: [icon.page.back] }
  page.heroes.rufus: { heroes: [icon.page.back] }
  page.heroes.seigel: { heroes: [icon.page.back] }
  page.heroes.seo_yoon: { heroes: [icon.page.back] }
  page.heroes.sergey: { heroes: [icon.page.back] }
  page.heroes.smith: { heroes: [icon.page.back] }
  page.heroes.sonya: { heroes: [icon.page.back] }
  page.heroes.ursar: { heroes: [icon.page.back] }
  page.heroes.viveca: { heroes: [icon.page.back] }
  page.heroes.vulcanus: { heroes: [icon.page.back] }
  page.heroes.wayne: { heroes: [icon.page.back] }
  page.heroes.wu_ming: { heroes: [icon.page.back] }
  page.heroes.xura: { heroes: [icon.page.back] }
  page.heroes.zinman: { heroes: [icon.page.back] }
