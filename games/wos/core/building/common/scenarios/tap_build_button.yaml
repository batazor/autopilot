enabled: true
name: 🏗️ Tap build button
device_level: true
steps:
  - while_match: build_button
    action: cta_button
    color: blue
    threshold: 0.5
    max: 1
    steps:
      - click: build_button
      - wait: 800ms
