screens:
  exploration:
    priority: 30
    rules:
    - match: exploration.to.squad_settings
      threshold: 0.9
  squad_settings:
    priority: 30
    rules:
    - match: squad_settings.title
      threshold: 0.9
  exploration.victory:
    priority: 20
    # Transient result screen; squad_fight scenario taps `next` to dismiss.
    terminal: true
    rules:
    - match: page.exploration.victory.title
      threshold: 0.9
  exploration.defeat:
    priority: 20
    rules:
    - match: exploration.defeat.title
      threshold: 0.9
