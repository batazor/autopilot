screens:
  chief_order:
    priority: 50
    rules:
      - match: icon.page.back
        threshold: 0.85

  chief_order_enact:
    priority: 55
    rules:
      - match: chief.enact
        threshold: 0.85
