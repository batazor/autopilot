screens:
  chief_order:
    priority: 50
    rules:
      - ocr: chief_order.title
        # RU guess «Приказ вождя» (token «Приказ») — VERIFY LIVE.
        contains: ["Chief Order", Приказ]

  chief_order_enact:
    priority: 55
    rules:
      - match: chief.enact
        threshold: 0.85
