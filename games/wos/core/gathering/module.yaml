id: gathering
title: Gathering
description: >-
  World-map resource gathering. Dispatches idle marches to gather the scarcest
  resource (meat / wood / coal / iron) and redeploys as marches return.
# enabled stays false until the gather-flow regions are labelled via the
# Labeling UI (search magnifier, resource tabs, tile Gather, deploy) and the
# `gather_resources` scenario is verified on a device. Flip to true then.
enabled: false
scenarios: scenarios
analyze: analyze/analyze.yaml
area: area.yaml
references: references
wiki: false
