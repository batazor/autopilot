id: main_city
title: Main City (Kingshot)
# Kingshot stub introduced in Phase 5 of the multi-game migration.
# This module exists so the worker can boot with `device.game = "kingshot"`
# and the scenario loader finds *something*. Real Kingshot modules are
# authored separately once the labeling/area data is captured.
wiki: false
