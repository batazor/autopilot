# Generated by the labeling UI — do not hand-edit.
# Capture a Kingshot `main_city` screenshot in /labeling, draw the
# `button.alliance.help` region (action: exist), save → this file and a crop
# under references/crop/ are written. Then flip module.yaml `enabled: true`.
version: 2
screens: []
